import { ImageURISource } from 'react-native';

export class Icons {
  static readonly retryIcon: ImageURISource = require('./icons/retry.png');
  static readonly useIcon: ImageURISource = require('./icons/use.png');
}
