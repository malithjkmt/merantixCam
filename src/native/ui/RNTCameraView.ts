import { requireNativeComponent } from 'react-native';
// requireNativeComponent automatically resolves 'RNTCameraView' to 'RNTCameraViewManager'
export const RNTCameraView = requireNativeComponent('RNTCameraView');
